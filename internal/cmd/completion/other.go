//+build !darwin

package completion

const longDescription = `To load completion run

. <(3scale-graphql-pa completion)

To configure your bash shell to load completions for each session run:

echo ". <(3scale-graphql-pa completion)" >> ~/.profile

`
